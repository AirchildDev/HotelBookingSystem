name: Hotel Booking System Deployment

on:
  workflow_run:
    workflows: ["Hotel Booking System CI"]
    types:
      - completed

jobs:
  deploy:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      - name: Prepare application
        run: |
          echo "Preparing Hotel Booking System..."
          echo "Deployment successful."

      - name: Deployment status
        run: |
          echo "Hotel Booking System deployment completed."